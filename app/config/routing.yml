submit_quote:
    path: /api/quotes
    methods:
        - POST
    defaults:
        _controller: MBApplicationBundle:Quote:submit

list_quotes:
    path: /api/quotes
    methods:
        - GET
    defaults:
        _controller: MBApplicationBundle:Quote:list